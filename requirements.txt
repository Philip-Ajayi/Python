# Core async and web
fastapi==0.110.2        # or latest stable
uvicorn[standard]==0.30.0
httpx==0.27.0
websockets==12.0

# Redis client (async)
redis==5.0.3             # async support in redis-py v5+

# Google Gemini SDK
google-genai==0.6.1      # or latest from PyPI

# Audio processing
pyaudio==0.2.13          # May require system dependencies to install

# Image processing
pillow==10.3.0           # For PIL.Image
mss==9.0.1               # For screen capture

# Optional: Logging and typing are standard libraries, no install needed
